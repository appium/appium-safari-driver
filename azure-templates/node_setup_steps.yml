steps:
  - task: NodeTool@0
    inputs:
      versionSpec: "$(DEFAULT_NODE_VERSION)"
  - script: sudo xcode-select --switch "/Applications/Xcode_$(XCODE_VERSION).app/Contents/Developer"
  # - script: safaridriver --enable
